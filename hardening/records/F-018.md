# Challenge record — F-018: `until` could name a metric nothing produces

```text
Challenge:                  curriculum-events (twin `until-unknown-metric`)
Source/paper:               curriculum / early-stopping schedules (§22, §26 item 8)
TENSA features stressed:     `until <metric> <cmp> <value>` as a phase stopping rule
TENSA implementation:
    phase easy {
      epochs 10
      until val_acc > 0.9        # no loss or validation ever produces `val_acc`
      update main with opt
    }
Check result (before):      ok. The rule was stored verbatim in the IR; the runtime
                            (once it honoured `until`, H-001) and the emitted plan
                            looked the metric up per step, found nothing, and ran the
                            phase to its `epochs` budget — an early-stopping rule that
                            never fires, with no message.
Check result (after):       AXS0709 error at the rule: "phase 'easy': `until val_acc`
                            names a metric no loss or validation produces", listing the
                            metrics that exist (`main`, `train_main`, `val_main`).
Runtime result:             (un-mutated) `until main < 0.9` ends phase `easy` before its
                            epoch budget; `report.phases[0].stoppedBy === "until"`.
Reference result:           emitted plan: `if metrics.get("main") is not None and
                            metrics["main"] < 0.9: break`.
Finding classification:     F (a name in the lifecycle grammar was not resolved)
Severity:                   major — silent no-op stopping rule
Workaround:                 none short of reading the run report
Proposed action:            DONE — `analyze.ts` phase `until`: resolve the metric against
                            `{loss, train_<loss>, val_<loss>}` for every declared loss.
Language change required?:  no
Regression test added?:     yes — challenge `curriculum-events` twin
                            `until-unknown-metric` (expects AXS0709)
```
