# Challenge record — F-005: `merge mean` lowered to the reduction op `mean`

```text
Challenge:                  topology-nesting `OuterNamed` (semantic.ts)
Source/paper:               —  (§7.1 split/merge)
TENSA features stressed:     topology lowering (§7.1), IR op vocabulary, both backends
Expected semantics:         `split merge mean { … }` averages the branch results
                            element-wise: output shape = branch shape
TENSA implementation:
    dim B
    model M(x: Tensor[B, 8]) -> Tensor[B, 8] {
      let skip = linear(8)
      gelu
      split merge mean { linear(8) ; branch b { linear(8) + skip } }
    }
Check result:               ok (the checker computed the merged type correctly)
IR result (before):         merge node op `mean` — the same name as the axis reduction
Runtime result (before):    interpreter executed the *reduction* on the first branch
                            (dropping the second) → wrong shape / wrong value
Emit result (before):       `.mean(dim=…)` on one branch
IR result (after):          ops `merge_add` / `merge_mean` (n-ary joins)
Runtime result (after):     [2, 8], sum of branches scaled by 1/n
Emit result (after):        `(vA + vB) / 2`
Finding classification:     F (op-name collision between two different semantics)
Severity:                   major — silent wrong numerics
Proposed action:            DONE — new IR ops in analyze/exec/emit
Language change required?:  no
Regression test added?:     yes — challenge topology-nesting: irOps merge_mean/merge_add,
                            emitContains ") / 2", emitForbids ".mean(", run output [2, 8]
```
