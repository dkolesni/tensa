# Challenge record — F-019: a model input that the body never read was silent

```text
Challenge:                  lm-padded (twin `mask-not-passed`)
Source/paper:               padded language modelling; the pad mask is a MODEL INPUT,
                            not a batch-derived heuristic (§20, §21)
TENSA features stressed:     multi-input models, `Mask` inputs, `attention(mask:)`
TENSA implementation:
    model LM(ids: Tokens[B, T], pad: Mask[B, 1, T]) -> Logits[B, T, V] {
      let h = ids |> embed(V, D)
      let a = h |> rmsnorm |> attention(heads: 2, causal: true)    # `pad` forgotten
      let r = h + a
      return r |> rmsnorm |> linear(V)
    }
Check result (before):      ok. `pad` was a declared input with a checked shape, bound
                            from the data field, threaded through the emitted `forward`
                            signature — and never used. The model attended over padding
                            with no diagnostic anywhere.
Check result (after):       AXS0305 warning at the model: "model 'LM' never reads its
                            input 'pad'", and because the input is a `Mask` the note
                            says where it belongs: "pass it to the operation it guards,
                            e.g. `attention(mask: ...)`".
Runtime result:             n/a (warning); the un-mutated program's attention node has
                            the mask as input 3 (F-013 slot rule).
Reference result:           PyTorch: an unused `forward` argument is ordinary Python.
Finding classification:     F (unused-input analysis missing; the contract "an input is
                            part of the computation" was not checked)
Severity:                   major for masks (silent wrong attention), minor otherwise
Workaround:                 —
Proposed action:            DONE — `analyze.ts` model elaboration walks the graph
                            (including regions) and warns for every input id no node
                            reads and the result does not return.
Language change required?:  no
Regression test added?:     yes — challenge `lm-padded` twin `mask-not-passed`
                            (warnCodes AXS0305); tests.ts `data / leakage is reported at
                            the fitting operation, and unused model inputs are named
                            (F-019, F-020)`
```
