# Challenge record — F-010: a misspelled objective port vanished silently

```text
Challenge:                  objective-graph twin `misspelled-port` (semantic.ts)
Source/paper:               —  (§16)
TENSA features stressed:     objective binding contract (§16), diagnostic honesty (§36)
Expected semantics:         every named argument of a loss binding must be a port of
                            the objective; a typo is an error at the typo
TENSA implementation:
    train J { … loss cls = Cls(logit: Net(x), labels: label) … }     # `logit`, not `logits`
Check result (before):      only AXS0503 "port 'logits' is not supplied" — true, but
                            the actual mistake (`logit`) was never mentioned; with an
                            *extra* misspelled argument alongside a complete binding
                            there was no diagnostic at all
Check result (after):       AXS0503 "objective 'Cls' has no port 'logit'" (note lists the
                            ports) at the argument, plus the existing "not supplied"
Finding classification:     F (silent acceptance of an unknown name)
Severity:                   minor
Proposed action:            DONE — unknown named arguments are reported before ports
                            are resolved
Language change required?:  no
Regression test added?:     yes — twin misspelled-port
```
