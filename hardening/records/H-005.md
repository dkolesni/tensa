# Challenge record — H-005: the emitter printed `-1` for symbolic extents and dropped the attention mask

```text
Challenge:                  mha-explicit / cross-attention-padding / slicing-torture
                            (tensor.ts) — emit layer
Source/paper:               —  (§12 backend consistency, §45 tensor completeness)
TENSA features stressed:     reshape / slice / causal_mask / one_hot / attention with
                            symbolic dims in the generated PyTorch
TENSA implementation:        (mha-explicit — see records/mha-explicit.md)
Emit result (before):       every reshape extent that was not a constant printed as `-1`:
                            `x.reshape(-1, -1, 2, 8)` — two inferred axes, invalid in
                            torch. Slices with symbolic bounds printed `[:, 0:-1]` (wrong
                            meaning). `causal_mask(T)` lowered to `?? 1`, `one_hot` to a
                            `?? 2` placeholder. The attention `mask` port was not emitted
                            at all — `F.scaled_dot_product_attention(q, k, v,
                            is_causal=False)` for a padded cross attention.
Emit result (after):        `forward` binds every plain runtime dim from the input shapes
                            before the body:
                                dims = resolve_dims({**self.dims, "B": q.shape[0],
                                                     "T": q.shape[1], "S": kv.shape[1]})
                            and every extent goes through `dr(d)` = constant if evaluable,
                            else `pyDim(d, v => dims["v"])`:
                                .reshape(dims["B"], dims["T"], 2, 8)
                                [:, 0:dims["T"] - 1, :]
                                torch.triu(torch.ones(dims["T"], dims["T"], dtype=torch.bool), 1)
                            Attention with a mask:
                                m = ~vv4.bool()[:, None]        # rank 3 → [B, 1, Tq, Tk]
                                m = m & torch.tril(...)         # only when causal: too
                                att = F.scaled_dot_product_attention(q, k, v, attn_mask=m)
                            (rank-2 masks use `[None, None]`; TENSA true = blocked, SDPA
                            true = allowed, hence `~`). Without a mask the previous
                            `is_causal=` form is kept.
Reference result:           the hand-written PyTorch in records/mha-explicit.md
Finding classification:     H (backend)
Severity:                   major — generated module did not run for any head split with
                            a symbolic axis, and silently ignored padding masks
Workaround:                 before: none short of editing the generated file
Proposed action:            DONE — `emit_torch.ts` `emitModule` (dims binding from input
                            shapes; `dims = self.dims` when nothing is bound), `emitNode`
                            `dr()` for reshape / slice / causal_mask / one_hot / attention
                            heads / scan carries; `pyDim` and `dims.ts` `show()` print the
                            constant term last (`T - 1`, `2*D + 6`).
Language change required?:  no
Regression test added?:     yes — backend / "runtime dims are bound from the inputs;
                            reshape, slice and masks never lower to -1 (H-005)";
                            challenge:mha-explicit emit (`emitForbids: reshape(-1`),
                            challenge:cross-attention-padding emit (`attn_mask=m`,
                            forbids `is_causal=False)`), challenge:slicing-torture emit
```
