# Challenge record — H-002: gradient coverage reported only the last update's gradients

```text
Challenge:                  objective-graph (semantic.ts), run layer with gradAll
Source/paper:               —  (§16 objectives, §17 gradient coverage)
TENSA features stressed:     multi-objective plans with several optimizers over
                            different parameter regions
Expected semantics:         a parameter "received a gradient" if any update step in the
                            run produced one for it; the coverage report is over the
                            whole run
TENSA implementation:        three objectives, three optimizers `over Net / A / Bm`,
                            three losses in one phase
Runtime result (before):    coverage listed every parameter not touched by the *last*
                            update as "no gradient path", although earlier updates in
                            the same step had reached them (zero_grad between updates
                            cleared `p.g`). The bundled multi-objective example showed
                            the same false report.
Runtime result (after):     `everGrad` set accumulated after every backward; coverage
                            reads it. objective-graph: all 3 models' parameters updated.
Finding classification:     H (reference runtime reporting)
Severity:                   major — a false "no gradient path" is exactly the class of
                            silent failure the language exists to surface
Proposed action:            DONE (exec.ts)
Language change required?:  no
Regression test added?:     yes — challenge:objective-graph run layer (gradAll)
```
