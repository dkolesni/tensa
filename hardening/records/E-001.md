# Challenge record — E-001: same-scope `let` rebinding was silent

```text
Challenge:                  identity-paths (semantic.ts), model Rebound; F-001 regression program
Source/paper:               —  (§8 parameter identity, §36 diagnostic honesty)
TENSA features stressed:     `let` scoping, stage binding, parameter identity paths
Expected semantics:         rebinding a name in one scope is legal; the earlier value is
                            unreachable and a rebound stage owns fresh parameters
                            (F-001). The author should be told, because the two readings
                            "same stage, shared weights" and "new stage" are both plausible.
TENSA implementation:
    model Rebound(x: Tensor[B, 8]) -> Tensor[B, 8] {
      let e = E()
      let y = e(x)
      let e = E()          # ← here
      return e(y)
    }
Check result (before):      no diagnostic
Check result (after):       warning[AXS0304] 'e' is rebound in the same scope; the earlier
                            binding is now unreachable
                              note: a rebound stage application gets its own parameters
                                    (it is not shared with the earlier one)
                              note: rename the binding if both values are needed
                            Scope = current region: a `let` inside `residual { }`,
                            `branch { }`, `for n:` or `scan { }` never warns about an
                            outer name.
Finding classification:     E (semantic ambiguity: valid source, two reasonable readings)
Severity:                   minor
Proposed action:            DONE — decision taken by the author 2026-09-17: warn, do not
                            forbid. Diagnostic only; no grammar or semantics change.
Language change required?:  no
Regression test added?:     yes — parameters / "rebinding a let name in the same scope
                            warns; nested regions do not (E-001)"; challenge identity-paths
                            expects AXS0304
```
