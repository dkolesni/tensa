# Challenge record — F-008: tuple-returning model bound to an objective port without an index

```text
Challenge:                  objective-graph twin `tuple-without-index` (semantic.ts)
Source/paper:               —  (§16 objectives as graphs)
TENSA features stressed:     objective binding (§16), tuples
Expected semantics:         a port receives one tensor; `Net(x)` returning a pair must
                            say which output — `Net(x)[0]` — or be rejected
TENSA implementation:
    model MT(x: Tensor[B, 16]) -> (Logits[B, K], Tensor[B, 1]) { … }
    train J { … loss cls = Cls(logits: Net(x), labels: label) … }
Check result (before):      ok — the binding type became the tuple, the port check
                            compared nothing useful and the runtime picked output 0
Check result (after):       AXS0408 "'Net' returns (Logits[B, 4], Tensor[B, 1]); select
                            the output to bind, e.g. Net(...)[0]"
Finding classification:     F (missing check in the contract layer)
Severity:                   major — binds an arbitrary output silently
Proposed action:            DONE — `bindingType` rejects an un-indexed tuple
Language change required?:  no
Regression test added?:     yes — twin tuple-without-index
```
