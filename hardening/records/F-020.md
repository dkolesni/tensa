# Challenge record — F-020: leakage was reported at the data declaration, not at the fitted op

```text
Challenge:                  tabular-pipeline (twins `impute-fit-all`, `impute-no-fit`),
                            vision-pipeline (twin `fit-on-val`)
Source/paper:               train/eval statistic leakage (§19)
TENSA features stressed:     fitted data operations (`impute`, `standardize`, `normalize`,
                            `vocab`) with `fit: <split>`
TENSA implementation:
    field age: Tensor[1] = select(column: "age") |> to_float
                           |> impute(median, fit: all) |> standardize(fit: train)
Check result (before):      AXS0620 was raised — at the `data` declaration's location.
                            With three fields and five fitted statistics the message
                            named the field but the caret sat on line 1 of the block; in
                            the twin corpus every leakage twin pointed at the same line.
Check result (after):       AXS0620 at the `impute(...)` call itself (line 6 in the
                            tests.ts case); the diagnostic reads "impute for field 'age'
                            is fitted on 'all' — statistics must be fitted on the train
                            split" with the caret on the op.
Runtime result:             n/a
Reference result:           n/a (PyTorch has no data-plane contract)
Finding classification:     F (diagnostic location; the check existed)
Severity:                   minor
Workaround:                 search the field pipeline for `fit:`
Proposed action:            DONE — `analyze.ts`: `fitted` entries carry the op's `loc`
                            (stripped before they reach `mod.data`), and the AXS0620
                            emission uses it.
Language change required?:  no
Regression test added?:     yes — `data / leakage is reported at the fitting operation,
                            and unused model inputs are named (F-019, F-020)` asserts
                            the line; challenge twins `impute-fit-all`, `impute-no-fit`,
                            `fit-on-val`
```
