# Challenge record — F-004: attention over a free width skipped the divisibility check

```text
Challenge:                  divisibility-ladder / divisibility-violated (semantic.ts)
Source/paper:               —  (§5.4 divisibility)
TENSA features stressed:     divisibility (§5.4), carried constraints (§5.2)
Expected semantics:         heads must divide D: constant D → exact (AXS0407 on
                            failure); free D → carried and verified when bound
TENSA implementation:
    dim B
    dim T
    dim D
    model M(x: Tensor[B, T, D]) -> Tensor[B, T, D] { attention(heads: 4) }
Check result (before):      ok, no diagnostics at all — the head split was checked
                            only when D was a constant; a free D was silently accepted
Check result (after):       warning AXS0403 "attention head split (heads must divide
                            D): cannot prove 4*⌊D/4⌋ = D"
Runtime result (after):     D=10 → "carried constraint violated … 8 ≠ 10", refuses;
                            D=8 runs
Reference result:           PyTorch: assertion `embed_dim % num_heads == 0` at construction
Finding classification:     F (missing constraint = false certainty)
Severity:                   major — the exact case §5.4 names as the target
Proposed action:            DONE — catalog attention `infer` records
                            `c.eq(dMul(dh, heads), D, …)` when D or D/heads is symbolic;
                            `eqDim` no longer binds a template dim to an expression
                            containing itself (needed for this constraint)
Language change required?:  no
Regression test added?:     yes — op-shapes attention free-D case requires AXS0403;
                            challenges divisibility-ladder (+ twin constant-indivisible),
                            divisibility-violated
```
