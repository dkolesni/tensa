# Challenge record — F-015: one model declaration bound under two aliases read as two copies

```text
Challenge:                  ema-teacher (twin `one-decl-two-aliases`)
Source/paper:               mean-teacher / BYOL style EMA teacher (§18 state, §26 item 5)
TENSA features stressed:     `model <alias> = <decl>` in a training plan, parameter identity
TENSA implementation:
    train Mean {
      model student = Student
      model teacher = Student        # meant as "a second copy"
      track shadow = ema(student, decay: 0.99)
      ...
    }
Check result (before):      ok, silently. `plan.models` held two aliases of one
                            declaration; the IR, the runtime and the emitted module all
                            (correctly) had ONE parameter set, so `teacher(x)` and
                            `student(x)` were the same network and the "teacher" trained
                            with the student. Nothing said so.
Check result (after):       AXS0706 warning at the second binding: "model 'Student' is
                            bound as both 'student' and 'teacher'; the two aliases share
                            one parameter set", with the two readings spelled out
                            (declare two `model`s that each instantiate the block, or
                            drop the alias if one set applied twice is meant).
Runtime result:             unchanged — one parameter set; the un-mutated challenge
                            declares `model Teacher(x) { Body() }` and `model Student(x)
                            { Body() }` and the paramOwners check proves they are
                            disjoint tables.
Reference result:           the emitted plan prints `teacher = student  # one model
                            declaration is one parameter set … (AXS0706, F-015)` so the
                            Python reads the same way the IR does.
Finding classification:     F (parameter identity was right but unannounced; a
                            newcomer reads two aliases as two copies)
Severity:                   major — training the teacher by accident is a silent
                            experiment failure
Workaround:                 two model declarations of one block
Proposed action:            DONE — `analyze.ts` plan model bindings warn; emitter aliases
                            the second name to the first instance.
Language change required?:  no (surfaces an existing rule)
Regression test added?:     yes — challenge `ema-teacher` twin `one-decl-two-aliases`
                            (warnCodes AXS0706) and the un-mutated `paramOwners` check
```
