# Challenge record — F-003: floor-division atoms printed without grouping

```text
Challenge:                  op-shapes table (src/lang/challenges/ops.ts), conv2d stride 2
Source/paper:               —  (§5.3 per-operator symbolic table)
TENSA features stressed:     symbolic dims rendering (§5.1), diagnostic honesty (§36)
Expected semantics:         a stride-2/pad-1 3×3 conv over H produces ⌊(H + 1)/2⌋
TENSA implementation:
    dim B
    dim H
    model M(x: Tensor[B, 3, H, H]) -> Tensor[B, 8, H / 2, H / 2] { conv2d(8, kernel: 3, stride: 2, pad: 1) }
Check result (before):      warning text "cannot prove ⌊H - 1/2⌋ = ⌊H/2⌋"  — reads as
                            H − ½, and two different atoms could print identically
Check result (after):       "cannot prove ⌊(1 + H)/2⌋ = ⌊H/2⌋" (operands with more than
                            one term are parenthesised)
Inspect / IR result:        same text (atom keys are the printed form)
Runtime result:             unaffected
Finding classification:     F (misleading rendering; atom identity depended on it)
Severity:                   minor
Proposed action:            DONE — `dims.ts` `atomKey` groups multi-term operands
Language change required?:  no
Regression test added?:     yes — op-shapes: conv2d stride cases assert the exact text;
                            challenge algebra-proved `Stride` output shape
```
