# Challenge record — F-006: zero repetitions produced no value

```text
Challenge:                  repetition-forms `Zero` (semantic.ts)
Source/paper:               —  (§9 static repetition, edge case)
TENSA features stressed:     static repetition (§9), implicit flow (§6)
Expected semantics:         `for 0: L()` is the identity: no parameters, output = input
TENSA implementation:
    dim B
    model M(x: Tensor[B, 8]) -> Tensor[B, 8] { for 0: linear(8) }
Check result:               ok (checker already treated the loop as identity)
Runtime result (before):    `static_repeat` with no regions set no output → the graph
                            result was undefined and the interpreter threw
Emit result (before):       no assignment for the output value → NameError in PyTorch
Runtime result (after):     [2, 8], input passed through
Emit result (after):        `o = a()  # zero repetitions: identity`
Finding classification:     F (backend-visible bug from a checker-accepted program)
Severity:                   minor
Proposed action:            DONE — exec/emit treat a region-less `static_repeat` as identity
Language change required?:  no
Regression test added?:     yes — challenge repetition-forms (Zero run output,
                            emitContains "zero repetitions: identity")
```
