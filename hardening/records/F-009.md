# Challenge record — F-009: "unknown" shape was modelled as a rank-1 tensor `[?]`

```text
Challenge:                  unknown-shape (semantic.ts)
Source/paper:               —  (§5.2 outcome table: exact / carried / impossible / unknown)
TENSA features stressed:     custom ops without shape semantics, honesty of the checker
Expected semantics:         after `shape: unknown` nothing downstream may be sized,
                            constrained or contradicted; the result contract is
                            reported as uncheckable, not as violated
TENSA implementation:
    dim B
    dim T
    dim D = 8
    custom op mystery(x: Tensor[B, T, D]) { effects: pure  shape: unknown }
    model M(x: Tensor[B, T, D]) -> Tensor[B, T, D] { return mystery(x) |> linear(D) |> gelu }
Check result (before):      AXS0903 (correct) followed by a *spurious error*
                            AXS0406 "'M' declares Tensor[B, T, 8] but produces Tensor[8]
                            (rank differs)" — the unknown was `tensor([dVar("?")])`, a
                            rank-1 value, so `linear` sized a 1×8 weight from it and the
                            result check compared ranks
Check result (after):       AXS0903 warning + AXS0903 info "result of 'M' cannot be
                            checked against Tensor[B, T, 8]: its shape is unknown";
                            output type `Tensor[?]`; 0 parameters, 0 constraints
Finding classification:     F (invented certainty; §5.2 names "unknown" as its own outcome)
Severity:                   major — a legal program was rejected with a false claim
Proposed action:            DONE — `TensorType.unknown` flag; catalog application,
                            binary ops, result and block-input checks propagate it
                            without reasoning about a shape
Language change required?:  no
Regression test added?:     yes — shapes / "unknown shape does not become a rank (F-009)";
                            challenge unknown-shape (+ twin declared-shape-restores-checking)
```
