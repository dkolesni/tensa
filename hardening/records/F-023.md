# F-023 — coherent tuple projections in a loss

Challenge: VAE/diffusion tuple bindings.
Source/paper: reparameterization/noise-prediction objectives.
TENSA features stressed: `m(x)[0]`, `m(x)[1]` in one loss.
Expected semantics: projections refer to the same tuple-valued forward within that loss, including draws/state effects.
TENSA implementation: exec projectionCache; generated projected(cache,key,forward,index). Cache is created/cleared per loss evaluation, including validation. Unindexed calls stay independent; separate losses and optimizer updates never share cached tensors/tapes.
Check result: accepted source; this was an execution error, not a missing syntax feature.
Inspect result: tuple result and indexed bindings unchanged.
IR result: no grammar/AST changes.
Runtime result: previously two samples; now one tuple evaluation. Regression returns (y,y) with noisy y and asserts three consecutive MSE losses exactly zero.
Reference result: both backends use the same scope; emitted VAE/diffusion plans run on CUDA.
Gradient result: both tuple branches reach one forward graph; no stale graph reused after stepping.
Diagnostics: none new.
Finding classification: F, fixed 2026-09-18.
Severity: major (wrong stochastic objective).
Workaround: none required.
Proposed action: retain cache-lifetime regression and document the projection/application distinction.
Language change required?: no; explicit tuple projection coherence, not general common-subexpression elimination.
Regression test added?: backend “tuple loss projections… (F-023)”.
