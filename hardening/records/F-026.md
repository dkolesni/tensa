# F-026 — class indices are not silently repairable

Challenge: RL one_hot failed on CUDA while reference run passed.
Source/paper: categorical policy objective.
TENSA features stressed: synthetic Class data, one_hot and cross_entropy.
Expected semantics: invalid class indices refuse; action identity must never be changed to make execution succeed.
TENSA implementation: shared tensor.classIndex validates finite integer range for one_hot/CE. guessVocab uses conservative consumer bounds instead of arbitrary 10 (minimum known embedding/CE/one_hot cardinality, 1 if unresolved).
Check result: shape checking cannot prove runtime value bounds; synthetic data remains a simulation.
Inspect result: no inferred target relationship is added.
IR result: unchanged.
Runtime result: negative/out-of-range/fractional/nonfinite labels refuse instead of clamping. Fixture labels fall in a safe common domain, not a claim of realistic class distribution.
Reference result: RL CUDA plan now runs with valid actions. PyTorch integer out-of-range labels already refuse.
Gradient result: gradients no longer correspond to a silently different class.
Diagnostics: runtime class-index error, no new static code.
Finding classification: F, fixed 2026-09-18.
Severity: major fidelity error.
Workaround: host must still supply valid labels; synthetic safe-domain heuristic is not a general data validator.
Proposed action: future per-port data-domain contracts separately from this fix.
Language change required?: no.
Regression test added?: backend F-026; GPU RL plan.
