# H-011 — GPU execution is the default requirement

Challenge: all emitted M4 programs, user requirement dated 2026-09-18.
Source/paper: execution policy, not an architecture.
TENSA features stressed: existing plan device setting, model/state allocation, constants, masks and batch transfer.
Expected semantics: use GPU unless CPU explicitly selected; no silent CPU fallback.
TENSA implementation: generated execution_device chooses CUDA then MPS, or raises. Explicit source `device cpu`, generated constructor/train `device="cpu"`, and harness `--device cpu` are opt-ins. Parameters/buffers initialize on target; graph constants/masks use local device; training and validation batches move recursively. A nonpersistent anchor supports parameterless modules and later module.to().
Check result: source language unchanged; device remains existing plan metadata.
Inspect result: device policy visible when specified.
IR result: no new device ops in model graphs.
Runtime result: reference interpreter is explicitly CPU-only validation (CLI/UI/docs labels), never evidence of a GPU run.
Reference result: torch 2.11.0+cu128, RTX 4090; nine compiling challenges, thirteen graphs, seven plans. Mocked absence of GPU raises; explicit CPU override is tested. MPS selection is implemented but not hardware-tested here.
Gradient result: GPU parameter gradients compared to explicit CPU oracle.
Diagnostics: clear Python RuntimeError for unavailable default GPU; no new AXS code.
Finding classification: H, fixed.
Severity: major execution-policy gap.
Workaround: explicit CPU only if requested; no implicit downgrade.
Proposed action: retain hardening/validate-m4.{ts,py} gate; mixed precision remains unsupported annotation.
Language change required?: no.
Regression test added?: backend H-011; executable GPU gate. Agent guidance now makes this requirement durable.
