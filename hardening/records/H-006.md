# Challenge record — H-006: emitted objectives raised NameError on their own ports

```text
Challenge:                  gan-alternating; also transfer-lifecycle (region lr),
                            every emitted plan with a named-port objective
Source/paper:               §25 emitted plans must run
TENSA features stressed:     objective functions with named ports, region learning
                            rates, optimizer regions over one of several models
TENSA implementation:
    objective GenLoss(fake_score: Tensor[B, 1]) -> Scalar { return mean(softplus(-fake_score)) }
    optimizer g_opt = adamw(lr: 2e-4) over G
    phase finetune { lr Net.backbone = 1e-5 ... }
Reference result (before):  `python -c "import L_gan"` compiled but the plan raised
                            `NameError: name 'v12' is not defined` inside
                            `objective_GenLoss`: the body referenced the IR value id of
                            the port while the function parameter was named after the
                            port. Two more emitter faults surfaced by the same run:
                              · optimizer parameter lists were built from the plan's
                                FIRST model variable (`D.p[...]` for the generator's
                                tables) instead of the owning alias — a KeyError, or
                                worse a silent wrong region when key names collided;
                              · per-region `lr` overrides printed a comment; there was
                                no `set_lr`, so a fine-tuning phase ran the backbone at
                                the head's rate.
Reference result (after):   objectives alias each port on entry (`v12 = fake_score`);
                            parameter lists are grouped by owning alias (see also
                            F-022); `set_lr(opt, params, lr)` moves a region into its
                            own param group. The torch harness runs all 20 examples
                            and 16 learning programs (19/19 trainable modules OK; the
                            `grad-three-way` module fails by design on the user-supplied
                            backend fn `weird`).
Runtime result:             unaffected (reference interpreter never went through Python)
Finding classification:     H (emitter)
Severity:                   major — emitted plans did not run
Proposed action:            DONE — `emit_torch.ts` objective prologue, `paramList`,
                            `set_lr`.
Language change required?:  no
Regression test added?:     yes — challenge `gan-alternating` custom check "objective
                            ports are aliased in the emitted Python (H-006)" and
                            `emitContains` `g_opt_params = [G.p[k]`; `transfer-lifecycle`
                            `emitContains` `head_opt_params = [Net.p[k] for k in
                            ["Transfer_head_linear…`
```
