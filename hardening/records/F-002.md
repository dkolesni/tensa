# Challenge record — F-002: carried constraints were never verified at runtime

```text
Challenge:                  carried-holds / carried-violated (src/lang/challenges/semantic.ts)
Source/paper:               —  (§5.2 outcome table: "carried → verified when bound")
TENSA features stressed:     symbolic dims (§5.2), reference runtime honesty (§5.2, §44)
Expected semantics:         an `assumed` constraint is a promise: when the dimensions
                            are bound the runtime checks it and refuses on violation
TENSA implementation:
    dim B
    dim S
    dim T
    dim D = 8
    model M(a: Tensor[B, S, D], b: Tensor[B, T, D]) -> Tensor[B, S, D] { return a + b }
Check result:               ok, warning AXS0403 "cannot prove S = T … verified when
                            dimensions are bound at runtime"
Inspect result:             constraint `add broadcast axis 1` status assumed
IR result:                  constraint carried in mod.constraints
Runtime result (before):    dims S=5, T=3 — `add` threw a tensor-level broadcast error
                            deep in the interpreter (or, for reshape, silently viewed
                            the wrong element count); the warning's promise was false
Runtime result (after):     "carried constraint violated: add broadcast axis 1: S = T
                            but 5 ≠ 3 under the bound dimensions" + "refusing to
                            execute" — before any forward pass; S=T=4 runs
Reference result:           PyTorch raises at the op; TENSA must refuse earlier and in
                            the language's own vocabulary
Gradient result:            n/a
Diagnostics:                compile-time AXS0403 unchanged; runtime refusal is a report
                            error, not a diagnostic code
Finding classification:     F (runtime did not honour the checker's contract)
Severity:                   major — the §5.2 promise was printed but not kept
Workaround:                 none
Proposed action:            DONE — `Runtime.checkConstraints()` evaluates every
                            `assumed` constraint under `dimEnv`; `runProgram` refuses
                            before executing. `tensor.reshape` now also rejects an
                            element-count mismatch instead of viewing garbage.
Language change required?:  no
Regression test added?:     yes — shapes / "carried constraints are enforced before
                            execution (F-002)"; challenges carried-holds, carried-violated,
                            divisibility-violated, unet-odd-input
```
