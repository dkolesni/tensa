# TENSA hardening — findings ledger

Next free IDs after M4: F-031, H-012, E-014, G-cand-006.
M4 decisions and verification dated 2026-09-18; no commits or grammar changes.

One row per finding. Classification letters follow protocol §3
(`tensa_hardening_plan.md`); change control follows `tensa_hardening_execution.md`
(F → fix now with regression test; H → backend/runtime work; G → proposal in
`hardening/proposals/`, never a direct language change).

Status: `open` · `fixed` · `recorded` (known, not scheduled) · `proposal` · `wontfix`.

## Milestone 4 and revalidation (2026-09-18)

| ID | Class | Sev. | Status | Summary | Where | Record / test |
|----|-------|------|--------|---------|-------|---------------|
| H-010 | H | major | fixed | VAE/diffusion lacked tensor sampling. Added `randn_like`: standard-normal floats, independent draws in train AND eval, no template gradient. Catalog vocabulary, not syntax. | catalog/tensor/exec/emitter | [records/H-010.md](records/H-010.md) · `challenge:vae` H-010 regression · ops table |
| H-011 | H | major | fixed | Generated PyTorch ignored device policy. User requirement: GPU by default, no CPU fallback. CUDA/MPS selection, GPU parameters/state/constants/masks, train/validation batch transfer; explicit `device cpu` / Python `device="cpu"` only. Reference interpreter explicitly CPU validation. | emitter / CLI / UI / agent guidance | [records/H-011.md](records/H-011.md) · backend H-011 regression · GPU harness |
| F-023 | F | major | fixed | Tuple loss projections reran stochastic/stateful models: diffusion prediction and target came from different draws. Cache projected calls for ONE loss evaluation; separate updates/losses remain fresh, unindexed calls independent. Both backends fixed. | exec evalBinding / emitter projected | [records/F-023.md](records/F-023.md) · backend F-023 regression · VAE/diffusion GPU plans |
| F-024 | F | major | fixed | Synthetic `Mask` fields were Gaussian floats; reference `>0.5` and torch bool conversion disagreed. Fixtures now contain only 0/1. | exec syntheticField | [records/F-024.md](records/F-024.md) · backend F-024/F-025 regression · encoder-decoder GPU comparison |
| F-025 | F | major | fixed | Fully blocked catalog-attention rows produced uniform context via finite-sentinel softmax, unlike SDPA's zero output/gradient. Zero masked probabilities after softmax. | exec attention | [records/F-025.md](records/F-025.md) · backend F-024/F-025 regression |
| F-026 | F | major | fixed | Reference one_hot/CE silently clamped invalid classes; synthetic classes defaulted to 10 regardless of classifier width, hiding invalid RL actions that failed on CUDA. Refuse invalid indices; conservative synthetic class domain from catalog consumers (not target inference). | tensor classIndex / exec guessVocab, one_hot | [records/F-026.md](records/F-026.md) · backend F-026 regression · RL GPU plan |
| F-027 | F | major | fixed | eqDim/leDim assumed every floor atom nonnegative. `1+floor((1-T)/2)` can be zero or negative; valid equality could be refuted and invalid bounds proved. Recursive conservative sign bounds now carry uncertainty. | dims lowerBound / analyze leDim | [records/F-027.md](records/F-027.md) · property F-027 (25 seeded instances) |
| F-028 | F | major | fixed | Numerical metamorphic oracle accepted NaNs (`NaN > tol` is false) and equal-sized different-shaped outputs. It now rejects non-finite results, runtime errors and shape differences. | challenges/metamorphic | [records/F-028.md](records/F-028.md) · metamorphic F-028 regression |
| F-029 | F | major | fixed | MoCo GPU state comparison exposed reference BN storing biased running variance. Forward normalization stays population variance; running estimate uses N/(N-1), matching torch; singleton training is refused. | exec batchnorm | [records/F-029.md](records/F-029.md) · backend F-029 regression · GPU state comparison |
| F-030 | F | major | fixed | Unknown-rank slicing treated the empty unknown shape vector as rank zero, raising false AXS0401 and inventing slice knowledge. Preserve unknown result and runtime slice descriptors; reference/emitter execute actual indexing. | analyze elabIndex / exec / emitter | [records/F-030.md](records/F-030.md) · shapes F-030 regression · unknown-bound property |
| E-011 | C | minor | fixed | Public docs claimed tensor select existed, EMA teachers were covered, observe returned old state, and precision placement was implemented. Corrected to actual boundaries; stale GAN example friction removed. | docs/examples | [records/revalidation-2026-09.md](records/revalidation-2026-09.md) |
| E-012 | C | minor | fixed | Bundled contrastive objective was named NTXent but only computed positive cosine alignment. Renamed Alignment and explicitly documented absence of negatives; M3 alignment is not full SimCLR. | examples contrastive-data | [records/revalidation-2026-09.md](records/revalidation-2026-09.md) · examples suite |
| E-013 | B | major | recorded | RL fixed-trajectory objective/update fits; environment/action sampling/rewards/returns and rollout refresh are host responsibilities. Dynamic training data is not implemented. | learning-system boundary | [records/rl-policy-value.md](records/rl-policy-value.md) |
| G-cand-004 | G? | — | proposal | Layout provenance: MHA merge and ViT patch ordering are shape-correct but numerically wrong without transposes. DenseNet channel order is a third ordering illustration, not independent proof that named axes solve it. Analysis only. | type/layout boundary | [proposals/layout-provenance.md](proposals/layout-provenance.md) · metamorphic E-007/G-cand-004 |
| G-cand-005 | G? | — | proposal | Cursor provenance: M1 side binding, DenseNet aux branch and VAE tuple destructuring recur. Existing explicit operands work; evidence supports visibility/tooling review, NOT an approved new cursor rule. | flow/inspect | [proposals/cursor-provenance.md](proposals/cursor-provenance.md) · flow E-003/G-cand-005 |

## Milestones 0–3 (historical ledger)


| ID | Class | Sev. | Status | Summary | Where | Record / test |
|----|-------|------|--------|---------|-------|---------------|
| F-001 | F | major | fixed | Rebinding a `let` name (`let y = Enc(x)` twice) silently **tied** the second application's parameters to the first (72 values instead of 144, `applications: 2`) — instance paths were keyed on the binding name with no uniquifier. | `analyze.ts` `instantiate()` | [records/F-001.md](records/F-001.md) · test `parameters / rebinding a let name does not tie parameters (F-001)` |
| F-002 | F | major | fixed | Constraints the checker could only *carry* (AXS0403, e.g. `S = T`) were never verified when the program ran — an execution with S=5, T=3 proceeded. Runtime now checks every assumed constraint against the dim bindings before any forward pass and refuses with the offending equation. | `exec.ts` `checkConstraints()` | [records/F-002.md](records/F-002.md) · `shapes / carried constraints are enforced before execution (F-002)` · challenge `carried-violated`, `unet-odd-input` |
| F-003 | F | minor | fixed | Floor-division atoms printed their operands without grouping: `⌊H - 1/2⌋` for ⌊(H−1)/2⌋. | `dims.ts` `atomKey` | [records/F-003.md](records/F-003.md) · challenge `algebra-proved` (Stride) |
| F-004 | F | major | fixed | `attention(heads: h)` over a *free* width skipped the `heads ∣ D` divisibility check (only constant widths were checked); D=10, heads=4 compiled. Now a carried constraint, enforced at runtime. | `catalog.ts` attention `infer` | [records/F-004.md](records/F-004.md) · challenge `divisibility-ladder`, `divisibility-violated` |
| F-005 | F | major | fixed | `split … merge mean` lowered to IR op `mean`, colliding with the *reduction* `mean` — the runtime and emitter reduced one branch instead of averaging branches. Distinct ops `merge_add` / `merge_mean`. | `analyze.ts`, `exec.ts`, `emit_torch.ts` | [records/F-005.md](records/F-005.md) · challenge `topology-nesting` |
| F-006 | F | minor | fixed | `for 0: …` (static repetition with zero regions) produced no value — downstream got a dangling cursor. Zero repetitions is now the identity. | `analyze.ts` `static_repeat` | [records/F-006.md](records/F-006.md) · challenge `repetition-forms` |
| F-007 | F | major | fixed | Destructuring `let (a, b) = …` did not move the implicit cursor; a following bare stage silently consumed the value from *before* the `let`. | `analyze.ts` cursor rule | [records/F-007.md](records/F-007.md) · challenge `flow-cursor-rules` (twin `destructure-then-bare`) |
| F-008 | F | major | fixed | Binding a tuple-returning model to an objective port without an index (`Cls(logits: MT(x))`) was accepted; the first element was taken silently. Now AXS0408 at the binding. | `analyze.ts` loss bindings | [records/F-008.md](records/F-008.md) · challenge `objective-graph` (twin `tuple-without-index`) |
| F-009 | F | major | fixed | `shape: unknown` custom ops produced a rank-1 tensor `[?]`, so `linear` sized a weight from it and the result check raised a *false* AXS0406. Unknown is now its own outcome (`Tensor[?]`): nothing downstream is sized, constrained or contradicted; the result contract is reported as uncheckable (AXS0903 info). | `types.ts` `TensorType.unknown`, `analyze.ts` | [records/F-009.md](records/F-009.md) · `shapes / unknown shape does not become a rank (F-009)` · challenge `unknown-shape` |
| F-010 | F | minor | fixed | A misspelled named argument in a loss binding (`logit:` for `logits:`) was dropped silently — only the derived "port not supplied" was reported, and nothing at all when the binding was otherwise complete. Now AXS0503 at the argument, listing the ports. | `analyze.ts` loss bindings | [records/F-010.md](records/F-010.md) · challenge `objective-graph` (twin `misspelled-port`) |
| F-011 | F | major | fixed | Slice and index bounds were never checked: `x[:, 0:T+1, :]` compiled, was typed `Tensor[B, T + 1, 8]` and *ran*, reading past the operand; `x[:, T, :]` likewise. Bounds are now proved (`1 ≤ T`, `T − 1 ≥ 0` are silent), refuted (new **AXS0410** for constant overshoot / index at extent / start after end) or carried as `≤` constraints (`W + 2 ≤ T`, AXS0403) that the runtime verifies before any forward pass; the runtime slice itself also refuses to read out of range. | `analyze.ts` `leDim`, `elabIndex`; `ir.ts` `Constraint.rel`; `exec.ts` | [records/F-011.md](records/F-011.md) · `shapes / slice bounds are proved, refuted or carried as ≤ constraints (F-011)` · challenge `slicing-torture`, `window-bound-runtime` |
| F-012 | F | major | fixed | The `attention` `mask:` port had no shape rule at all and `masked_fill` checked only the mask's *kind*; at runtime a rank-3 mask trailing-broadcast its batch axis onto the *heads* axis of `[B, H, Tq, Tk]`. Contract now: `Mask[Tq, Tk]` or `Mask[B\|1, Tq\|1, Tk]` (checked with `eq`, so symbolic cases are carried and constants refuted); `masked_fill` checks trailing broadcast; the runtime lifts rank-3 masks to `[B, 1, Tq, Tk]`. | `catalog.ts` attention / masked_fill, `exec.ts` | [records/F-012.md](records/F-012.md) · `shapes / attention and masked_fill check the mask shape (F-012)` · challenge `cross-attention-padding` |
| F-013 | F | major | fixed | Optional ports were appended in *order of appearance*, not port position: `attention(query: x, mask: m, heads: 4)` put the mask in the **key** slot (the mask became K and V; the checker then complained about "key width"). A skipped optional port now defaults to the port before it (key ← query, value ← key), so the mask always reaches input 3. | `analyze.ts` catalog call, optional ports | [records/F-013.md](records/F-013.md) · `shapes / optional ports keep their position… (F-013)` · pair `causal-flag-vs-explicit-mask` |
| E-001 | E | minor | fixed | `let` shadowing in one scope was accepted without any diagnostic. After F-001 the semantics are "independent application, earlier binding unreachable", but nothing told the author. Decision (2026-09-17): warn. New **AXS0304** "name rebound in scope" at the second `let`, with a note that a rebound stage gets its own parameters. Branch / residual / repeat / scan bodies are fresh scopes and do not warn. | `analyze.ts` `Frame.bound`, `types.ts` | [records/E-001.md](records/E-001.md) · `parameters / rebinding a let name in the same scope warns; nested regions do not (E-001)` · challenge `identity-paths` |
| E-003 | E | — | recorded | A side `let` (`let aux = x |> linear(4)` with no later use) *redirects* the implicit cursor: the next bare stage consumes `aux`, not `x`. This is the documented rule ("implicit value = most recent statement") and is now pinned by a twin, but it is the most surprising reading of §6 for newcomers. Design note; no change proposed. | `analyze.ts` cursor rule | challenge `flow-cursor-rules` (twin `let-redirects-cursor`) |
| E-004 | E | minor | recorded | Binding a `Probs` value to a `Logits` objective port is only a *warning* (AXS0602). Plain `Tensor` → `Logits` is silent by design (kinds are refinements). Whether softmax-then-cross-entropy deserves an error is a design decision. | `analyze.ts` kind check | challenge `objective-graph` (twin `kind-mismatch-model`) |
| E-005 | E | minor | fixed | Same-sign polynomial differences (`2K` vs `4K`) were *carried* (AXS0403) although `2K − 4K` can never be zero for K ≥ 1 — the U-Net wrong-level skip was accepted pending runtime. `eqDim` now refutes any difference whose terms all share a sign and include a plain-variable monomial. | `dims.ts` `isNonZero`, `analyze.ts` `eqDim` | `shapes / same-sign polynomial differences are refuted…` · challenge `unet-skip` (twin `skip-from-wrong-level`) |
| E-006 | E | cosmetic | fixed | `merge add` / `merge mean` reported branch width mismatches as AXS0401 while `concat` used AXS0405, so the same mistake got a different code depending on the merge. All merges now use AXS0405 with a rank check. | `analyze.ts` split merge | challenge `topology-nesting` (twin `add-merge-width-mismatch`) |
| H-001 | H | major | fixed | Training schedules / `until` / events / region lr / `clip_grad` / tracked shadows were in the IR but not simulated by the reference runtime. Opened as "known, not scheduled" in M1; became blocking for the M3 lifecycle challenges. The training loop now simulates the whole plan (one epoch = 6 synthetic steps) and the RunReport carries `phases[{name, steps, stoppedBy, lrStart, lrEnd}]` and `events[{step, phase, action, detail}]`. | `exec.ts` training loop, `emit_torch.ts` `emitPlan` | [records/H-001.md](records/H-001.md) · `lifecycle / plan-level events count run steps… (H-001, H-008)` · challenges `baseline-ceremony`, `curriculum-events`, `transfer-lifecycle` |
| H-002 | H | major | fixed | Gradient coverage reflected only the *last* update's gradients (zero_grad between updates cleared them), so multi-objective plans reported false "no gradient path" for parameters reached by earlier updates — including the bundled multi-objective example. | `exec.ts` coverage report | [records/H-002.md](records/H-002.md) · challenge `objective-graph` (run, gradAll) |
| H-003 | H | major | fixed | Emitted PyTorch put derived dims in the module header as bare Python (`STATIC_DIMS = {"H": 4*K}` → NameError) and looked non-constant shapes up as `dims["4*K"]` (KeyError); symbolic state shapes silently became `1`. Symbolic extents now lower to Python expressions over the dims dict; derived dims are resolved after runtime bindings. | `emit_torch.ts` `pyDim`, `resolve_dims` | [records/H-003.md](records/H-003.md) · `backend / symbolic extents lower to Python expressions… (H-003)` |
| H-004 | H | minor | fixed | Catalog gap: no spatial upsampling op, so a U-Net could not be written without a custom op. Added `upsample(scale)` as library vocabulary (`[B,C,H,W] → [B,C,H·s,W·s]`, runtime + emitter). Not a language change. | `catalog.ts`, `tensor.ts` | challenge `unet-skip` · ops table `upsample` |
| E-007 | E | — | recorded | Merging heads *without* the transpose back (`reshape(B, T, D)` straight from `[B, Heads, T, DH]`) is shape-correct — `Heads·T·DH = T·D` — so no shape rule can see it. The checker is not wrong; this is exactly the class of layout error §12 says only a metamorphic pair can catch. Recorded as the boundary of static checking; the `mha-manual-vs-catalog` pair fails on it. M4 ViT patchify adds a second concrete witness → G-cand-004 layout-provenance analysis (not approved named-axis syntax). Also folded in: `show()` / `pyDim` now print the constant term last (`T - 1`, `2*D + 6`) — cosmetic. | `analyze.ts` reshape rule, `dims.ts` `show` | [records/E-007.md](records/E-007.md) · `backend / a layout error the shape rules cannot see is caught by the metamorphic pair (E-007)` |
| H-005 | H | major | fixed | The emitter printed `-1` wherever a `reshape`/`slice` extent was symbolic (a rank-4 head split with two symbolic axes produced an invalid `view(-1, -1, …)`), lowered `causal_mask` to `?? 1` and `one_hot` to `?? 2`, and **dropped the attention `mask` port entirely**. `forward` now binds runtime dims from the input shapes (`dims = resolve_dims({**self.dims, "B": x.shape[0], …})`) and every extent goes through `dr()`; masks lower to `attn_mask=~m` (TENSA true = blocked, SDPA true = allowed) combined with `tril` when `causal:` is also set. | `emit_torch.ts` `emitModule`, `emitNode` | [records/H-005.md](records/H-005.md) · `backend / runtime dims are bound from the inputs… (H-005)` · challenge `mha-explicit`, `slicing-torture` (emit) |
| G-cand-001 | G? | — | recorded | M4 sparse MoE rejected; seven-case dynamic disposition separates eager selection, bounded scan, conditional execution and host algorithms. Not promoted: conceptual early-exit/adaptive witnesses do not yet specify effect/gradient joins. | IR/control-flow boundary | [records/cond-disposition.md](records/cond-disposition.md) · `challenge:moe-topk` |
| G-cand-002 | G? | — | proposal | explicit `grad` / gradient-as-value (WGAN-GP penalty, MAML, and — M3 — adversarial training's `∇ₓ L`): three witnesses that want three different things (parameter gradient, gradient penalty, input gradient). M4 §43 analysis separates all three; higher-order retention, parameter-set evaluation and effects remain unresolved. No `grad` added; MAML skipped with reason. | differentiation boundary | [proposals/differentiation.md](proposals/differentiation.md) · `challenge:wgan-gp` |
| F-014 | F | major | fixed | A phase could `update` with an optimizer whose every parameter that phase had frozen — a warmup that trained nothing, silently. Now AXS0704 at the phase. | `analyze.ts` phase checks | [records/F-014.md](records/F-014.md) · challenge `transfer-lifecycle` (twin `frozen-but-updated`) |
| F-015 | F | major | fixed | `model teacher = Student` next to `model student = Student` read as two copies; it is one parameter set (correctly, everywhere) and nothing said so. New **AXS0706** warning at the second alias; the emitter aliases the second name to the first instance. | `analyze.ts` plan model bindings, `emit_torch.ts` | [records/F-015.md](records/F-015.md) · challenge `ema-teacher` (twin `one-decl-two-aliases`) |
| F-016 | F | major | fixed | A `fn` application (and every region-owning node: residual, parallel, scan, static_repeat) was `pure` regardless of its body — a fn calling `dropout` passed the "no randomness at eval" reasoning. Container nodes now carry the union of their body's effects. | `analyze.ts` `node()`, `inspect.ts` | [records/F-016.md](records/F-016.md) · challenge `fn-effects` |
| F-017 | F | major | fixed | An optimizer that claimed parameters but no phase applied, and a loss no phase updated, compiled silently (GAN with the generator update forgotten). New **AXS0708** warnings. | `analyze.ts` plan checks | [records/F-017.md](records/F-017.md) · challenge `gan-alternating` (twin `generator-never-updated`) |
| F-018 | F | major | fixed | `until val_acc > 0.9` could name a metric nothing produces; the rule never fired and the phase ran its budget. New **AXS0709** error listing `{loss, train_<loss>, val_<loss>}`. | `analyze.ts` phase `until` | [records/F-018.md](records/F-018.md) · challenge `curriculum-events` (twin `until-unknown-metric`) |
| F-019 | F | major | fixed | A model input the body never read (a pad `Mask` not passed to `attention`) was silent. New **AXS0305** warning; for a Mask the note says where it belongs. | `analyze.ts` model elaboration | [records/F-019.md](records/F-019.md) · challenge `lm-padded` (twin `mask-not-passed`) · `data / leakage is reported at the fitting operation… (F-019, F-020)` |
| F-020 | F | minor | fixed | AXS0620 (statistic not fitted on train) pointed at the `data` declaration, not at the fitted op. Now at the op. | `analyze.ts` `fitted[].loc` | [records/F-020.md](records/F-020.md) · challenges `tabular-pipeline`, `vision-pipeline` twins |
| F-021 | F | major | fixed | A field pipeline could not start from an earlier field by name (`field inputs: Tokens[T] = ids[0 : T]` → AXS0204 "unknown data operation"); the gpt example only worked because `window` is also a data op. Earlier fields now lower to `field.<name>`; unknown names still warn. | `analyze.ts` `dataPipeline(earlierFields)` | [records/F-021.md](records/F-021.md) · `data / a field may be built from the fields declared before it… (F-021)` · challenge `lm-padded` |
| F-022 | F | major | fixed | An optimizer without `over` covered only the FIRST model bound in the plan (`plan.models[0]`); the second model back-propagated and never moved, runtime and emitter alike. Region-less optimizers now have region `*` = every parameter of every bound model; the emitted list is grouped by owning alias. | `analyze.ts` `resolveRegion`, `exec.ts` `paramsOfRegion`, `emit_torch.ts` `paramList` | [records/F-022.md](records/F-022.md) · `lifecycle / a region-less optimizer covers every model in the plan… (F-022)` |
| H-006 | H | major | fixed | Emitted objectives raised `NameError` on their own ports (body used the IR value id, the function parameter was the port name); optimizer parameter lists were built from the plan's first model variable; region `lr` overrides were a comment. Ports are aliased on entry, lists are grouped by owning alias, `set_lr` moves a region into its own param group. All 16 learning programs run under torch 2.12. | `emit_torch.ts` objectives, `paramList`, `set_lr` | [records/H-006.md](records/H-006.md) · challenge `gan-alternating` (custom H-006 check), `transfer-lifecycle` (emit) |
| H-007 | H | major | fixed | Nested loss bindings `D(G(z))` were parsed as `<model>(<field>)` by the runtime — the inner model got a fresh synthetic tensor, never went on the tape, and coverage reported a false "no gradient path" for the generator; the emitter printed `batch["G(z"]`. Bindings now lower recursively (model calls, tuple indices, field names) with one synthetic tensor per field name per step. | `exec.ts` `evalBinding`, `emit_torch.ts` | [records/H-007.md](records/H-007.md) · challenge `gan-alternating` (run gradAll, emitForbids), `distillation`, `self-training` |
| H-008 | H | minor | fixed | Runtime fired plan-level `every N steps` on the phase step; the emitter fired phase-level events on the global step. Convention: phase events count steps within the phase (`phase_step`), plan events across the run (`step`); both backends agree. | `exec.ts` `fireEvents`, `emit_torch.ts` `emitEvents` | [records/H-008.md](records/H-008.md) · `lifecycle / plan-level events count run steps, phase events count phase steps… (H-001, H-008)` · challenge `curriculum-events` |
| H-009 | H | minor | fixed | Catalog gaps in the data plane: no `pad_to` / `pad_mask` (padded LM batches) and no `noise` augmentation for feature vectors. Added as library vocabulary (`DATA_OPS`), not language. | `catalog.ts` `DATA_OPS` | challenges `lm-padded`, `self-training` |
| E-008 | E | minor | fixed | A refuted axis between a data field and a model input (HWC vs CHW, 1 vs 3 channels) got AXS0401 or AXS0602 depending on which axis differed. Decision (2026-09-17): data↔model refutations are AXS0602, model↔objective AXS0603, with the axis named; AXS0401 is for clashes inside a graph. | `analyze.ts` compatibility loop (`eqDim` code parameter) | [records/E-008.md](records/E-008.md) · challenge `vision-pipeline` (twins `wrong-layout`, `wrong-channels`, `target-kind`) |
| E-009 | E | minor | fixed | `ema(r)` never said whether `r` is the fraction kept (EMA/Polyak convention) or the fraction of the new value (BatchNorm "momentum"). Decision (2026-09-17): `r` is the DECAY — `next = r·cur + (1−r)·new`; one `emaDecay` helper is shared by runtime and emitter. | `exec.ts` `emaDecay`, `emit_torch.ts` | [records/E-009.md](records/E-009.md) · challenge `ema-teacher` (emit `mul_(0.99).add_(…, alpha=0.01)`), `state-zoo` |
| E-010 | E | — | recorded | AXS0901 (custom op with a torch backend but no reference implementation; the runtime substitutes the identity) is **info**, not a warning — a legitimate program should compile clean. Pinned by `grad-three-way` (`warnCodes: []` + custom check). | `analyze.ts` custom ops | [records/grad-three-way.md](records/grad-three-way.md) |
| G-cand-003 | G? | — | proposal | **Derived parameter sets** — `track … = ema(…)` produces a shadow only checkpoints can see; no model can be *evaluated with* it (EMA teacher), be assigned from another set (teacher refresh), or run at a temporarily derived θ' (MAML). Three witnesses; §43 analysis written, no syntax, bar 6 (BN statistics of the source vs the derived set) concretely hit by M4 MoCo and still open. | — | [proposals/derived-parameter-sets.md](proposals/derived-parameter-sets.md) · [records/escalation-ladder.md](records/escalation-ladder.md) · [records/ema-teacher.md](records/ema-teacher.md) |
